- let allHex = hexData

svg(
    class="hex_map"
)
    each hex in allHex
        path(
            d=hex.d
            code=hex.code
        )
        text(
            text-anchor="middle"
            x=hex.tx
            y=hex.ty
        )=""
        //hex.text

